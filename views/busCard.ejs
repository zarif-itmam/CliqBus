<link rel="stylesheet" href="/css/routecard.css">
<style>
    .rounded-circle-sm-img{
     width: 50px;
     height: 50px;
     object-fit: cover;
     border-radius: 50%;
    }
</style>
    <!-- busCard -->
    <div class="row">
        <div class="col-md-4">
            <div class="card p-3 mb-2" style="height: 300px;width: 350px; border-radius: 15px;">
                <div class="d-flex align-items-center mt-2" style="height: 70px;">
                    <!-- <% if(bus.BUS_RELATED_MEDIA===null) { %>
                        <img src="/images/anonymous.webp" class="rounded-circle-sm-img">
                    <% } else { %>
                        <img src="<%= bus.BUS_RELATED_MEDIA %>"
                        class="rounded-circle-sm-img">
                    <% } %> -->
                    <h4 class="heading">&nbsp;License Plate: <%= bus.LICENSE_PLATE %></h4>
                </div>
                <div class="mt-2">                
                    <h4 class="heading">&nbsp;Type: <%= bus.BUS_TYPE %></h4>
                    <h4 class="heading">&nbsp;Seat Count: <%= bus.SEAT_COUNT %></h4>
                    <h4 class="heading">⭐<%= bus.BUS_RATING.toFixed(2) %></h4>
                </div>

                <div class="mt-2" style="height: 50px;">
                    <form action="/bus-company-profile/delete" method="POST">
                        <input type="hidden" name="busId" value="<%= bus.BUS_ID %>">
                        <button type="submit" id="delete-bus" name="delete-bus"
                            style="margin-bottom: 5px; margin-top: 5px;"
                            class="btn btn-danger btn-sm m-2"
                        >Delete</button>
                    </form>
                </div>
                
            </div>
        </div>
    </div>
    <script>
        function truncateText(text, maxLength) {
            if (text.length <= maxLength) {
                return text;
            }
    
            const lastSpaceIndex = text.lastIndexOf(' ', maxLength);
            return text.slice(0, lastSpaceIndex) + ' ...';
        }
    
        document.addEventListener('DOMContentLoaded', function () {
            const commentElement=document.querySelectorAll('#truncatedComment');
            console.log(commentElement)
            commentElement.forEach((element) => {
                element.innerText = truncateText(element.innerText, 100);
            });
            // const commentElement = document.getElementById('truncatedComment');
            // commentElement.textContent = truncateText(commentElement.textContent, 100);
        });
    </script>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" >
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js" >
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js" >
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@latest/css/boxicons.min.css" >